export class Customer {
  constructor(public id: number, public name: string, public salesYtd: number, public growth: number, public salesLastYear: number, public state: string, public promoPercentage: number){}
}
